name: martha
 
on: [pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: checkout code
        uses: actions/checkout@v4
        
      - name: setup nodejs
        uses: actions/setup-node@version4
        with:
          node-version: '22.14'

      - name: install dependencies
        run: npm install

      - name: run test
        run